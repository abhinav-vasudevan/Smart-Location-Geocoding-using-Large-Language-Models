# Core ML / NLP
torch>=2.0.0
transformers>=4.30.0
sentencepiece>=0.1.99

# NLP tooling
spacy>=3.7.0
nltk>=3.8.1
rapidfuzz>=3.0.0

# Data
pandas>=2.0.0

# Database
psycopg2-binary>=2.9.6

# HTTP (Ollama client)
requests>=2.31.0

# API server
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0

# Environment / secrets
python-dotenv>=1.0.0

# ── Post-install steps ────────────────────────────────────────────────────
# 1. Download spaCy English model:
#    python -m spacy download en_core_web_sm
#
# 2. NLTK resources are auto-downloaded at first run, or run:
#    python -c "import nltk; nltk.download('averaged_perceptron_tagger_eng'); nltk.download('punkt_tab')"
#
# 3. Ollama (for LLaMA3) is a separate install: https://ollama.com/download
#    After install: ollama pull llama3
