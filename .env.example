# Copy this file to .env and fill in your values
# The application reads these via os.getenv() (python-dotenv is optional)

DB_NAME=Address_corrector
DB_USER=postgres
DB_PASSWORD=your_postgres_password_here
DB_HOST=localhost
DB_PORT=5432
